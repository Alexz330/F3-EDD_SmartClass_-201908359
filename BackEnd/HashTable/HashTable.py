import array

# class HashTable:
#     def __init__(self,size):
#         self.data  = array(size)
    
#     def hashMethod(self, key):
#         hash =  0
#         for i in key:
#              hash = (hash + ord(i)*i ) % len(self.data)
#         return hash
    
#     def set(self,key,value):
#         address  = self.hashMethod(key)

#         if (self.data[address] is None):
#             self.data[address] = []
#         self.data[address].push([key, value])
#         return self.data
    

    

# myHashTable = HashTable(50)

# myHashTable.set("Alexis",2001)

# for i in myHashTable.data:
#     print(i)

ccode = array.array.typecodes()

